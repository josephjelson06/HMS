[pytest]
asyncio_mode = auto
markers =
    integration: requires a live database and running migrations
    system: black-box tests against a running backend (set SMOKE_BASE_URL)
filterwarnings =
    ignore::DeprecationWarning
